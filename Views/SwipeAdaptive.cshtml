@*

    Swipe adaptive view

    Switches between desktop and mobile views depending on device

    DOESN'T WORK because caching gets in the way. I've left it here for historical reasons only.

*@

@if (Request.Headers["User-Agent"].ToLower().Contains("phone"))
{
    @Display.SwipeMobile(Model)
}
else
{
    @Display.PhotoSwipe(Model)
}
